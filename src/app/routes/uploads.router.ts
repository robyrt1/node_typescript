// import path  from "path";
// import express from "express";

// export const UploadsRouter = express.Router();

// UploadsRouter.get("/uploads/:image", (req, res) => {
//   // const result = express.static(path.resolve(__dirname, "../../..", "uploads"));
//   // console.log(result);
//   res.status(200).sendFile(express.static(path.resolve(__dirname, "../../..", "uploads")););
// });
